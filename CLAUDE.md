# Archon Sequential Test Repo

- This repository exists to test local Archon workflow orchestration.
- For story-readiness work, use local Archon workflows from `.archon/workflows`.
- Discover workflows with `archon workflow list --cwd "$PWD"`.
- Do not assume GitHub issues exist here; the local workflows read `user-stories/*.md`.

